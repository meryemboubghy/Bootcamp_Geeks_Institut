create table countries(id SERIAL primary key ,
                       name varchar(100), 
					   capital varchar(100), 
					   flag varchar(255), 
					   subregion varchar(100), 
					   population int 
					   )

					   select * from countries